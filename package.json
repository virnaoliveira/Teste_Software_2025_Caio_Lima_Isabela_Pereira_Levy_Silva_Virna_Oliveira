{
  "name": "string-to-stream",
  "version": "1.0.0",
  "description": "Transforma uma string em um stream legível (Readable Stream)",
  "main": "src/stringToStream.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
